import 'package:flutter/material.dart';

class MeetingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Meeting")),
      body: Center(child: Text("Meeting Page")),
    );
  }
}
