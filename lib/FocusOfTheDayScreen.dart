import 'package:flutter/material.dart';

class FocusOfTheDayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Focus Of The Day Screen")),
      body: Center(child: Text("Focus Of The Day Screen")),
    );
  }
}
